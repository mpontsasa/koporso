#include"CollisionHandler.h"

/*
CollisionHandler& CollisionHandler::access()
{
    static CollisionHandler _CollisionHandler_;
    return _CollisionHandler_;
}

void CollisionHandler::checkCollision(ScreenObject* otherObject, Direction _direction, sf::Vector3i& newPoint)
{
    for(auto iter = collidableObjects.begin(); iter != collidableObjects.end(); iter++)
    {
            if( (*iter).collisionPoint(otherObject, _direction, newPoint) )
        {
            //lekezeles
        }
    }
}
*/
